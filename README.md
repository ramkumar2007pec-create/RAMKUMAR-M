# RAMKUMAR-M
A responsive, client-side web application that fetches and displays real-time weather metrics using modern asynchronous JavaScript and public REST APIs. Automatically detects user location via the Geolocation API and provides a fallback global search functionality with dynamic UI component transformations.
